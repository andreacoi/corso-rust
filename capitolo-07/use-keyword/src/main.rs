//tipi di inclusione file
//glob
use std::collections::*;
// nested con self
use std::io::{self, Write};
// il paragrafo sugli import con use non è difficoltoso e sono tutte nozioni semplici o già
// utilizzate in altri linguaggi.

fn main() {
    println!("Hello, world!");
}
